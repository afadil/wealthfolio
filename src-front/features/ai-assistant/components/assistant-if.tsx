import { MessagePrimitive, ThreadPrimitive } from "@assistant-ui/react";

// Re-export primitives for conditional rendering
export const ThreadIf = ThreadPrimitive.If;
export const MessageIf = MessagePrimitive.If;
